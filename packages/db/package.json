{
  "name": "@vaccitrack/db",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed:rf-1122n": "tsx scripts/seed-calendar-rf-1122n.ts",
    "db:seed:msk-207": "tsx scripts/seed-calendar-msk-207.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "tsx": "^4.19.0"
  }
}
